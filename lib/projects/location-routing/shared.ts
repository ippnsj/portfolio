import type { ProjectShared } from '../types';

export const locationRoutingShared: ProjectShared = {
  slug: 'location-routing',
  status: 'done',
  stack: ['Flutter', 'BLoC', 'Dart', 'New Relic', 'NRQL'],
};
