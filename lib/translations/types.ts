export interface Translations {
  header: {
    title: string;
  };
  hero: {
    name: string;
  };
  sections: {
    about: string;
    projects: string;
    skills: string;
  };
  about: {
    paragraphs: string[];
  };
  detail: {
    background: string;
    problem: string;
    keyDecisions: string;
    result: string;
    period: string;
    role: string;
  };
  footer: {
    sourceCode: string;
  };
  status: {
    inProgress: string;
    done: string;
  };
}
