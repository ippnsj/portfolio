export interface Translations {
  header: {
    title: string;
  };
  hero: {
    name: string;
  };
  sections: {
    projects: string;
    skills: string;
  };
  detail: {
    background: string;
    problem: string;
    keyDecisions: string;
    result: string;
    period: string;
    role: string;
  };
  footer: {
    sourceCode: string;
  };
  status: {
    inProgress: string;
    done: string;
  };
}
