export interface Translations {
  hero: {
    name: string;
    headline: string;
  };
  sections: {
    projects: string;
    skills: string;
  };
  detail: {
    background: string;
    problem: string;
    keyDecisions: string;
    result: string;
    period: string;
    role: string;
  };
  footer: {
    sourceCode: string;
  };
}
